use python 3.10

run infer_attn_lstm_realtime.py


# for infer_attn_lstm_realtime.py

press `c` to clear all the history

press `Space` to save and print the sectence

press `Backsapce` to delete the wrong input

press `Esc` to quit the ui

Remember to insert your DeepSeek API key in `Gloss2Sen.py`
